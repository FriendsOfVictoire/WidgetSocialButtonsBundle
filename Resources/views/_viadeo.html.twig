<a href="{{ item.url }}" title="{{ item.title }}" alt="{{ 'widget.socialbuttons.link'|trans({}, 'victoire') }} {{ item.kind }}" class="social-icon-{{ item.kind }}"{% if item.analyticsTrackCode %} onclick="{{ item.analyticsTrackCode }}"{% endif %}></a>
